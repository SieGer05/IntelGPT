# Core dependencies
groq
tqdm
uvicorn
fastapi
pydantic
requests
chromadb
python-dotenv

# ML/Embeddings - includes both SentenceTransformer (bi-encoder) 
# and CrossEncoder (reranker) support
sentence-transformers